version: 1
name: test
path: /tmp/toshokan
schema:
  time_field: timestamp
  mappings:
    timestamp:
      type: !datetime
        formats:
          - timestamp
        indexed: true
        stored: true
        fast: true
    tenant_id:
      type: !number
        type: u64
